//METHODS OF ARRAYS

const cars = new Array("Saab", "Volvo", "BMW");  or  const cars=[] 

//Add Values
cars.push("Saab");
cars.push("Volvo");
cars.push("BMW");

console.log(cars)

document.getElementById("demo").innerHTML = cars;


///// at  /////
const fruits = ["Banana", "Orange", "Apple", "Mango"];
let fruit = fruits.at(2);  //Apple

let fruit = fruits.at(); //Banana (first value)

let fruit = fruits.at(-1); //Mango (last value)


//// concat////

const arr1 = ["Cecilie", "Lone"];
const arr2 = ["Emil", "Tobias", "Linus"];
const arr3 = [1];
const children = arr1.concat(arr2, arr3);  //Cecilie,Lone,Emil,Tobias,Linus,1


// it also remove the nested array and print it as normal
const arr1 = [1, 2, [3 , 4]];
const arr2 = [[5, 6], 7, 8];

const arr3 = arr1.concat(arr2); 
console.log(arr3)  //  [1,2,[3,4],[5,6],7,8]
document.getElementById("demo").innerHTML = arr3;  ///121345675

//////

copyWithin() copies array elements to another position in an array, overwriting existing values

array.copyWithin(target, start, end)

const fruits = ["Banana", "Orange", "Apple", "Mango"];

document.getElementById("demo").innerHTML = fruits.copyWithin(2,0); // Banana,Orange,Banana,Orange

so it will start from 2nd index and copy the whole data

///////// 

const fruits = ["Banana", "Orange", "Apple", "Mango"];

// Create an Iterator
const list = fruits.entries();
console.log(list)

// List the Entries
let text = "";
for (let x of list) {
  text += x + "<br>";
}

console.log(text);

The entries() method returns an Iterator object with the key/value pairs from an array:

[0, "Banana"]
[1, "Orange"]
[2, "Apple"]
[3, "Mango"]

/////////
every()
// Create an Array
const ages = [32, 33, 16, 40];

// Create a Test Function
function checkAge(age) {
  return age > 18;
}

// Are all ages over 18?
ages.every(checkAge);

//////////////
The fill() method fills specified elements in an array with a value.
The fill() method overwrites the original array.

const fruits = ["Banana", "Orange", "Apple", "Mango"];
fruits.fill("Kiwi"); all 4 is Kiwi

const fruits = ["Banana", "Orange", "Apple", "Mango"];
fruits.fill("Kiwi", 2, 4); // index 2 and 3 

//////////////
The filter() method creates a new array filled with elements that pass a test provided by a function.
The filter() method does not execute the function for empty elements.
The filter() method does not change the original array.

const ages = [32, 33, 16, 40];
const result = ages.filter((e)=>{
    return e>32;
});

console.log(result)

/////////
The find() method returns the value of the first element that passes a test.
The find() method executes a function for each array element.
The find() method returns undefined if no elements are found.
The find() method does not execute the function for empty elements.
The find() method does not change the original array.

ages.find(checkAge) // only give us the first value that is bigger than 18

//////////
findIndex() returns the index of the first element that passes a test (provided by a function).
ages.findIndex(checkAge)

The findLast() method returns the value of the last element that passes a test.

//////
array.flat(depth)
The flat() method concatenates sub-array elements.

const myArr = [1, 2, [3, [4, 5, 6], 7], 8];
const newArr = myArr.flat(2);

////

The flatMap() method maps all array elements and creates a new flat array.

flatMap() creates a new array from calling a function for every array element.

const myArr = [1, 2, 3, 4, 5, 6];
const newArr = myArr.flatMap(x => [x, x * 10]); // [1,10,2,20,3,30...]

here if we use map the data willl be like [[1,10],[2,20]]

The forEach() method calls a function for each element in an array.
const fruits = ["apple", "orange", "cherry"];
let res='';
fruits.forEach((value)=>{
    res += value + " "
})

The includes() method returns true if an array contains a specified value.
arr.includes("Mango");

The indexOf() method returns the first index (position) of a specified value.
let index = fruits.indexOf("Apple");

array.indexOf(item, start)


////////////
The isArray() method returns true if an object is an array, otherwise false.
let result = Array.isArray(fruits);


////////////
The join() method returns an array as a string.

const fruits = ["Banana", "Orange", "Apple", "Mango"];
let text = fruits.join();  //Banana,Orange,Apple,Mango

let text = fruits.join(" and "); //Banana and Orange and Apple and Mango

///////
The keys() method returns an Iterator object with the keys of an array.
for (let x of fruits.keys()) {
  text += x + "<br>";
}

////////
The lastIndexOf() method returns the last index (position) of a specified value.
const fruits = ["Apple", "Orange", "Apple", "Mango"];
let index = fruits.lastIndexOf("Apple"); //2
let index = fruits.lastIndexOf("Apple",1);//0

/////////
let length = fruits.length;//4

//////
map() creates a new array from calling a function for every array element.

let ans=num.map((n)=>{
    return 2**n;
})

const persons = [
  {firstname : "Malcom", lastname: "Reynolds",age:30},
  {firstname : "Kaylee", lastname: "Frye",age:25},
  {firstname : "Jayne", lastname: "Cobb",age:20}
];

let ans = persons.map((val)=>{
    return val.firstname + " " + val.lastname ;
});

let ans2=persons.filter((val)=>{
    return val.age >22
}).map((val)=>{
    return val.firstname + " " + val.lastname;
})

console.log(ans)
console.log(ans2)


/////////
The Array.of() method creates a new array from any number of arguments.
let fruits = Array.of("Banana", "Orange", "Apple", "Mango");


/////////////
The pop() method removes (pops) the last element of an array.

const fruits = ["Banana", "Orange", "Apple", "Mango"];
fruits.pop();

The push() method adds new items to the end of an array.

fruits.push("Kiwi", "Lemon");

//////////////////
The reduce() method executes a reducer function for array element.
array.reduce(function(total, currentValue, currentIndex, arr), initialValue)

const numbers = [175, 50, 25];

total=0
var a=numbers.reduce((total,n)=>{
    return total -= n;
})

console.log(a)

////////////
The reduceRight() method executes a reducer function for each array element.

rest operator

let a, b, c, rest;
const arr1 = [1,2,3,4,5,6];

[a, b, c, ...rest] = arr1;

console.log(rest)  4,5,6


/////////////

The reverse() method reverses the order of the elements in an array.

const fruits = ["Banana", "Orange", "Apple", "Mango"];
fruits.reverse();

///
The shift() method removes the first item of an array.
fruits.shift();

The unshift() method adds new elements to the beginning of an array.

////////////
The slice() method returns selected elements in a new array.
array.slice(start, end)

const fruits = ["Banana", "Orange", "Lemon", "Apple", "Mango"];
const citrus = fruits.slice(1, 3);

console.log(citrus) //Orange,Lemon

///////////////
The some() method checks if any array elements pass a test (provided as a callback function).

const val=fruits.some((e)=>{ return e=="Lemon"})  // true

///////////////////The sort() method sorts the elements of an array.
The sort() method sorts the elements as strings in alphabetical and ascending order.

The sort() method overwrites the original array
fruits.sort();

/////////////////////

The slice() method returns selected elements in a new array.
The slice() method selects from a given start, up to a (not inclusive) given end.
The slice() method does not change the original array.

const fruits = ["Banana", "Orange", "Lemon", "Apple", "Mango"];
const citrus = fruits.slice(1, 3);

//////////////////////
The splice() method adds and/or removes array elements.

The splice() method overwrites the original array.

const fruits = ["Banana", "Orange", "Apple", "Mango"];

// At position 2, add "Lemon" and "Kiwi"
fruits.splice(2, 0, "Lemon", "Kiwi");

//////////////////SPREAD
const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];
const arr3 = [...arr1, ...arr2];

The ... operator can be used to copy arrays:

const arr1 = [1, 2, 3];
const arr2 = [...arr1];

/////////////////////////

The reverse() method reverses the order of the elements in an array.
The reverse() method overwrites the original array.

//////////////////////

The toReversed() method reverses the order of the elements in an array.
The toReversed() method returns a new array.
The toReversed() method does not overwrites the original array.
The toReversed() method is the copying version of the reverse() method.

///////////////////
The sort() method sorts the elements of an array.
The sort() method sorts the elements as strings in alphabetical and ascending order.
The sort() method overwrites the original array.


//////////////
The toSorted() method sorts the elements of an array in alphabetical order.
The toSorted() method returns a new array.
The toSorted() method does not overwrite the original array.
The toSorted() method is the copying version of the sort() method.
//////////////////////

The toString() method returns a string with array values separated by commas.
The toString() method does not change the original array.

/////////////////
The values() method returns an Iterator object with the values of an array.
The values() method does not change the original array.
////////////

The with() method updates a specified array element.
The with() method returns a new array.
The with() method does not change the original array.